# 本地 / 局域网单机安装 — 复制为 .env.local 后按需修改
NEXT_PUBLIC_REMOTE_SYNC=true
SYNC_API_KEY=请改成复杂密码
NEXT_PUBLIC_SYNC_API_KEY=请改成复杂密码
SYNC_STORAGE=file
SYNC_DATA_DIR=./data
PORT=3000
HOSTNAME=0.0.0.0
NODE_ENV=production
