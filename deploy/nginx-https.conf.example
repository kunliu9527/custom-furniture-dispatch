# HTTPS 示例（配合 Let's Encrypt 或阿里云下载的证书）
# 证书路径按 certbot 实际生成路径修改

server {
    listen 80;
    listen [::]:80;
    server_name dispatch.你的域名.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name dispatch.你的域名.com;

    # certbot 默认路径示例
    ssl_certificate     /etc/letsencrypt/live/dispatch.你的域名.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/dispatch.你的域名.com/privkey.pem;

    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 10m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
